# Wiring — UMFT200Xd (FT200XD)

Typical usage:
- USB to host
- I2C pins available as slave device on the module (check datasheet)
- CBUS pin is configurable (see `FT_Prog` screenshot in this folder)

Notes:
- Use provided drivers / DS_UMFT200XD.pdf for configuration.
- Confirm VBUS and IO voltage levels before connecting targets.